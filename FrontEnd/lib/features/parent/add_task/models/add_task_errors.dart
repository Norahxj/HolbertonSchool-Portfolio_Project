enum AddTaskErrorCode {
  selectAtLeastOneChild,
  selectTaskType,
  taskNameRequired,
  descriptionRequired,
  pointsRange,
  taskNameLength,
  descriptionLength,
}

enum AddTaskSaveErrorCode { generic }
